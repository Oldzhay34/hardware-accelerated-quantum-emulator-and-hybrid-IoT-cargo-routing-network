# Python
.venv/
__pycache__/
*.pyc

# IDE / editor
.idea/
.ipynb_checkpoints/

# Claude Code local state (bkz. Faz 0 setup notu — kimlik bilgisi içerebilir)
.claude/

# Sırlar — gerçek .env asla commit edilmez, yalnızca .env.example
.env
.env.*
!.env.example
!**/.env.example
!*.env.enc
*.pem
*.p12
*.jks

# age özel anahtarları — bunlar repoda DEĞİL, %APPDATA%\sops\age\keys.txt'te.
# Yine de kazara kopyalanma ihtimaline karşı desen olarak kapatılıyor.
# NOT: secrets.enc.yaml şifrelidir ve commit EDİLİR — buradaki desenler onu kapsamaz.
keys.txt
*.agekey
age-key*.txt

# NOT: data/synthetic/ commit EDİLİR (gitignore'da değil). Sentetiktir, gizlilik riski
# yoktur ve ölçümlerin dayandığı girdinin sabit kalmasını garanti eder.
# Bkz. docs/data-governance.md §2.3. Gerçek kişisel veri hiçbir koşulda commit edilmez.

# OSM dökümü — 44,6 MB, sürümü MD5 ile pinli (scripts/fetch_osm.ps1 doğrular).
# Yeniden indirilebilir olduğu için commit edilmez. Bkz. specs/001-.../research.md R-3
data/osm/
# OSRM ön işleme çıktısı — 282 MB, yeniden üretilebilir
data/osrm/

# NOT: data/matrices/ commit EDİLİR — ölçüm girdisinin sabit kalması için (Prensip II)

# pytest
.pytest_cache/

# Büyük ikili dosyalar — bkz. docs/repo-conventions.md §3 (harici depo kararı)
artifacts/bitstream/*
artifacts/overlay/*
artifacts/models/*
!artifacts/**/.gitkeep
!artifacts/README.md
*.bit
*.hwh
*.xclbin

# --- Faz 2: HLS çekirdeği -----------------------------------------------------
# Derleme ve sentez çıktısı; kaynaktan yeniden üretilebilir.
hls/build/
hls/*_prj/
hls/**/*.o
vitis_hls.log
*.log
# NOT: hls/src/, hls/tb/, hls/tcl/ ve hls/build_and_run.sh COMMIT EDİLİR.
# NOT: docs/measurements/ altındaki damgalı JSON/npy ölçüm çıktıları da
#      commit edilir — Prensip II, ölçüm kanıtı repoda kalır.

qir_hls_prj*/

# Vitis HLS'in depo kokune biraktigi artik
dfx_runtime.txt
