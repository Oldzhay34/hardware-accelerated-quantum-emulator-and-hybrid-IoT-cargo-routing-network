# agent/

PYNQ (Zynq PS) tarafında koşan `fpga-agent` servisi (Faz 5). Overlay yükleme, bitstream durumu, tünel üzerinden dışarıya sağlık ucu.

Kart bağlı değilken bu servis mock arayüzle test edilebilir olmalıdır (Prensip V).
